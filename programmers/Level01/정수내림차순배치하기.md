```javascript
function solution(n) {
  var answer = parseInt(
    String(n)
      .split("")
      .sort((a, b) => b - a)
      .join("")
  );
  return answer;
}

// sort 함수
// sort((a, b) => b-a) 내림차순
// sort((a, b) => a-b) 오름차순
```
