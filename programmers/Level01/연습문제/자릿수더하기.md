```javascript
function solution(n) {
  var answer = String(n)
    .split("")
    .map((str) => parseInt(str));
  var sum = 0;
  for (let i = 0; i < answer.length; i++) {
    sum += answer[i];
  }

  return sum;
}
```
